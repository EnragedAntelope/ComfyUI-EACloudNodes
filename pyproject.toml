[project]
name = "eacloudnodes"
description = "ComfyUI v3 nodes for cloud LLM services: Groq Chat, OpenRouter Chat, and an OpenRouter model browser. Live model lists, vision support, JSON output, seed control, and automatic retries."
version = "2.2.2"
license = {file = "LICENSE"}
dependencies = []

[project.urls]
Repository = "https://github.com/EnragedAntelope/ComfyUI-EACloudNodes"
#  Used by Comfy Registry https://comfyregistry.org

[tool.comfy]
PublisherId = "enragedantelope"
DisplayName = "ComfyUI-EACloudNodes"
Icon = ""
