[pytest]
# Kept inside tests/ on purpose: it pins pytest's rootdir here so pytest does not
# treat the repository root (a ComfyUI package whose __init__.py uses relative
# imports) as the collection root package.
addopts = --import-mode=importlib
